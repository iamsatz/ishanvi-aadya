/** Parent PIN for answers view and upload (light lock — change in config). */
export const PARENT_PIN = '1234';

export const APP_NAME = 'Learning Adventures';

/** School branding — set enabled:false to remove logo everywhere. */
export const SCHOOL_LOGO = {
  enabled: true,
  src: '/silver-oaks-logo.png',
  alt: 'Silver Oaks International School',
} as const;
